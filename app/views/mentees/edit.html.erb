<article class="xviews xmentees xshow container row">
</section>
		<section class="col s5">  
	     <% if current_user && user_type == "mentee" && get_user.id == @mentee.id %>
		<h2>Edit Profile</h2>
		<%= render 'form'%>

		<%= link_to 'Show', @mentee %> |
		<%= link_to 'Back', mentee_path %>
	<% else %>
		<p> This page is private. </p>
	<% end %>
	    </section>
		<section class="col s7 center card hoverable">
		</section>
</article>