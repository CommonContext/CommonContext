<article class="xviews xmentors xshow container row">
	<% if current_user && user_type == "mentor" %>
	<h3>My Profile</h3>
	<section class="col s5">
			<%= render 'mentor_profile' %>
		</section>
		<section class="col s7 center">
      <%= link_to "Create Appointment", new_mentor_appointment_path(@mentor) %>
		<% if @mentor.appointments.any? %>
			<h3>Upcoming Appointments</h3>
			<% @mentor.appointments.each do |appointment| %>
				<ul>
					<li>
						<%= link_to "#{appointment.datetime.strftime("%B %e, %Y at %I:%M %p")}", mentor_appointment_path(id: appointment.id, mentor_id: @mentor.id) %> <%# with #{appointment.mentee.first_name + " " + appointment.mentee.last_name}   We should be able to clean this up.%> 

					</li>
				</ul>

			<% end %>
	<!-- put else inside cone when uncommenting <%  %> -->
			<h3>View pending matches</h3>
			<!-- maybe something here to show pending matches that need scheduling confirmation -->
		<% end %>
	</section>
	<% else %>
	<p> This profile is private. </p>
	<% end %>
</article>

<!-- include private info, ie upcoming appts and pending matches -->
<!-- render partial with public info - photo, industry, title, etc -->
